<%- include("header") -%>
<div class="box">
    <div id="heading">
        
        <a href="/">todo</a>
      
    </div>
</div>
    <div class="box" id="heading">
        <h1> <%= listTitle %></h1>
    </div>
    <div class="box">
                <% for(var i=0; i<newTasks.length; i++){ %>
                    <form action="/delete" method="POST">
                    <div class="item">
                        <input type="checkbox" name="checkbox" onchange="this.form.submit()" value="<%= newTasks[i]._id %>">
                        <input type="hidden" name="listHeading" value="<%= listTitle %>"></input>
                        <p><%= newTasks[i].name %></p>
                    </div>
                </form>
                <% } %>
                <div class="item">
                    <form action="/" method="POST">
                        <input type="text" name="task" placeholder="add task" autocomplete="off">
                        <button type="submit" name="list" value=<%= listTitle %>>+</button>
                        </form>
                </div>
    </div>
   
 <%- include("footer") -%>